
export type SectionData = {
    id: string,
    heading: string,
    image: string,
    description: string,
    images: File[],
    page: string,
    service?: string, 
    location?: string,
    date_range?: string
}
